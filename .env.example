# Copy to .env and fill in your values. .env is gitignored.

# AWS region for Bedrock
AWS_REGION=us-east-1

# Authentication — choose ONE of the following:

# Option 1 (recommended): Bedrock API key — Bedrock-scoped, no IAM needed
# BEDROCK_API_KEY=your-bedrock-api-key-here

# Option 2: Scoped IAM user credentials — create an IAM user with only
# bedrock:InvokeModel and bedrock:ListFoundationModels permissions.
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...

# Option 3: For EC2/ECS, attach an IAM role to the instance/task.
# Leave all credential vars unset; Soto will use the metadata endpoint.

# Optional: require an API key on OpenAI-compatible endpoints
# (/v1/models and /v1/chat/completions). The Anthropic /v1/messages
# endpoint is always unauthenticated (Xcode Coding Agent manages its own auth).
# PROXY_API_KEY=your-proxy-api-key-here

# Optional overrides
# DEFAULT_BEDROCK_MODEL=us.anthropic.claude-sonnet-4-5-20250929-v1:0
# CROSS_REGION_PREFIX=global   # Prefix prepended to model IDs that support inference profiles.
#                              # Use: global (default), us, eu, or ap.

# Model list — copy models.json.example to models.json and edit to control
# which models appear in GET /v1/models (and are resolved by name in requests).
# When models.json is present the live AWS list is skipped entirely.
# models.json is gitignored; mount it as a volume in Docker (see docker-compose.yml).
